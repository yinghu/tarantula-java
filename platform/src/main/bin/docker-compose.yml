version: '3'
volumes:
  gc-mysql:
services:
  mysql:
    image: mysql:5.7
    environment:
      - "MYSQL_ROOT_PASSWORD=root"
    command: "--default-authentication-plugin=mysql_native_password"
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - "gc-mysql:/home/tarantula/mysql"