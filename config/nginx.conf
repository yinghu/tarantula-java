user  nginx;

events {
    worker_connections   1000;
}

http {
    upstream gamecluster {
        server 10.20.0.2:8090;
        server 10.20.0.3:8090;
    }

    server {
    	listen 4000;
        location / {
            proxy_pass http://gamecluster;
        }
    }
}